repos:
  - repo: local
    hooks:

    - id: ruff
      name: ruff
      entry: uv run ruff check --fix .
      language: system
      pass_filenames: false
      types:
        - file
        - python
